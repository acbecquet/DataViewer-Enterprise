# DataViewer Enterprise - v2.4.1 Acceptance / Test Checklist (regression-fix batch)

**Build under test:** `dist\DataViewer-setup.exe` (FileVersion 2.4.1.0) - branch `feature/v2.4.0-bugfix-batch`
**Supersedes** the v2.4.0 checklist. v2.4.0 was withdrawn after the save/sync regressions
(root-cause dossier: `docs/superpowers/specs/2026-06-10-v24-save-sync-regressions-evidence.md`).
All five root causes are fixed and covered by an automated E2E harness (`tests/tst_saveintegrity_e2e`,
five production-failure scenarios, all green) - this manual pass is the human confirmation layer.

## Prerequisites
- [ ] Installed v2.4.1; app reports **2.4.1**.
- [ ] Live NAS PostgreSQL reachable.
- [ ] First launch: `dataviewer.log` (`%LOCALAPPDATA%\SDR\DataViewer Enterprise\`) shows ensureSchema
      heal lines (files.added_at identity + dve_commit_cell_json numeric) once; later launches quiet.

## 1. Saves stick (the "reverted next day" fix)
- [ ] Sensory: edit several scores, Ctrl+U, close the app. Reopen + reload from DB -> ALL edits present.
- [ ] Same again but close via the session Close button instead of Ctrl+U -> edits present after reopen.
- [ ] Export/Excel save after editing -> the file contains the latest scores (not stale).
- [ ] TPM: edit cells, Ctrl+U, reopen -> edits present. The log contains NO "skipped - already up to date" lines (string removed from the codebase).
- [ ] Historical check: open an OLD sensory session whose scores previously "reset to 5" -> real scores now show (tolerant reader repairs string-typed rows).

## 2. No false "name already in use" + duplicates auto-suffix
- [ ] Rename a session to a name that already exists, Ctrl+U -> NO modal error; status bar notes it saved as "<name>_1"; the visible Test Title shows the suffixed name.
- [ ] Press Ctrl+U again twice -> no dialogs, no new rows (check DB browser: exactly the expected rows).
- [ ] Create two new sessions with identical test/tester/round and save -> second becomes _1; both in DB.

## 3. Close always offers options
- [ ] Close an unnamed session with data -> dialog offers **Name It Now / Discard Session / Cancel** (no forced-naming OK box).
- [ ] Discard -> session gone from panel AND from DB; app continues normally.
- [ ] Program exit with an unnamed session -> same options; Discard exits cleanly; Cancel keeps the app open.
- [ ] TPM close when DB unreachable -> **Retry / Close Anyway / Cancel**.

## 4. TPM versioned re-adds
- [ ] Load a TPM file, save, close. Load the SAME file fresh -> DB browser shows TWO versions of it, the newer one stamped "(added yyyy-MM-dd HH:mm)". Loading by path gets the newest.
- [ ] In-session re-saves do NOT mint new versions (row count stable while working).
- [ ] DB browser "remove duplicates" warning now explicitly says older versions will be deleted.

## 5. Sync health visibility
- [ ] Pull the network cable / kill VPN mid-edit, make edits, restore network -> either edits stream (worker reconnects) or the indicator shows "N edits not synced" amber; in EITHER case a Ctrl+U then reload shows the edits (dirty-cell merge).

## 6. Regression sweep (carried from v2.4.0 checklist)
- [ ] Sample-name dropdown still scoped to current test + height-capped (DV-2).
- [ ] Zero Save-As dialogs in sensory/detailed; blank test name/tester still blocks interactive save with guidance (DATAVIEWER-8).
- [ ] TPM load persists (DV-3); mode switching, reports, radar charts OK.
- [ ] `.\tests\deployment\Test-Deployment.ps1` all phases pass.

## Sign-off
- [ ] All sections pass -> approve wrap to **v2.5.0 deployable** (merge -> tag -> your Synology drop).
- [ ] Failures -> v2.4.2 internal iteration.

Tester: __________  Date: __________  Result: [ ] Pass  [ ] Pass-with-notes  [ ] Fail
